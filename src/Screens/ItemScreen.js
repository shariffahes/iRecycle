import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ItemScreen = () => {
    return (
        <View>
            <Text>Mabrook you are in</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
export default ItemScreen;